# Scripts

Operational Impect pipeline scripts have moved to **`private/impect-pipeline/`**
(local only, not pushed to GitHub). See `docs/documentation/data/impect_pipeline.rst`.

The files in this directory (`build_impect_spadl_h5.py`, etc.) are kept for reference
during migration; prefer the private copies for production use.
